# Vérification
docker --version

# Exécution
docker run <image>
docker run hello-world

# Inspection
docker ps -a
docker images
docker container ls -a

# Nettoyage
docker rm <id-conteneur>
docker rmi <id-image>
docker container rm <id>
docker image rm <id>
